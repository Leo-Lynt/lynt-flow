# pnpm workspace configuration

# Hoist dependências comuns para a raiz (economiza espaço)
hoist-pattern[]=*

# Hoist ferramentas de build/lint para a raiz (acessível globalmente)
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*
public-hoist-pattern[]=*vite*

# Manter isolamento (mais seguro que shamefully-hoist)
shamefully-hoist=false

# Symlink workspace packages (padrão, mas explícito)
link-workspace-packages=true

# Store global do pnpm
store-dir=~/.pnpm-store

# Strict peer dependencies (evita warnings)
strict-peer-dependencies=false
